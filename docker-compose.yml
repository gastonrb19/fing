services:
  postgres:
    image: postgres:12.4-alpine 
    container_name: fing-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fing
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d fing"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pg_data:
